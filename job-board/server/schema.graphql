type Query {
    jobs: [Job!]
}

type Company {
    id: ID!
    name: String!
    description: String
}

type Job {
    id: ID!
    title: String!
    description: String

    """The date when the job is published"""
    dateCreated: String!

    company: Company!
}